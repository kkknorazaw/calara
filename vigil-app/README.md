# Vigil App Snapshot

This folder stores the refactored React + TypeScript frontend snapshot created during the UI componentization pass.

Included:
- reusable UI and layout components
- typed mock data files
- strict TypeScript setup
- webpack-based static build config

Excluded:
- `node_modules`
- `dist`
- transient preview logs
